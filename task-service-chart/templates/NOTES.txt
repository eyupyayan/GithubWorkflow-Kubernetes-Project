1. Get the application pods:
  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/instance={{ .Release.Name }}"

2. Get the service:
  kubectl get svc -n {{ .Release.Namespace }} {{ include "task-service.fullname" . }}