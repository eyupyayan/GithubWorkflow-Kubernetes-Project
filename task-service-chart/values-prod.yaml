replicaCount: 2

config:
  appEnv: prod

autoscaling:
  minReplicas: 2
  maxReplicas: 4

resources:
  requests:
    cpu: "150m"
    memory: "192Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"